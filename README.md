# Abdullah Ahmad Khan — Research Website

A responsive, dependency-free academic portfolio for research, teaching, publications and projects.

## Run locally

Open `index.html` directly, or run a tiny local server:

```bash
python -m http.server 8000
```

Then open `http://localhost:8000`.

## Deploy

This is a static site, so it can be deployed directly to GitHub Pages, Vercel, Netlify, Cloudflare Pages or any static host.

## Files

- `index.html` — page content and SEO metadata
- `styles.css` — visual system and responsive layout
- `script.js` — reveal motion, mobile nav, scroll progress and interactive unlearning visual
- `assets/Abdullah_Ahmad_Khan_CV.pdf` — CV download

## Notes before public launch

1. Confirm volatile manuscript statuses before publishing.
2. Replace the GitHub avatar with a high-resolution professional portrait if desired.
3. Add ORCID only after confirming the exact public profile URL.
4. Keep private/admin/visa/payroll details off the public site.
